import 'package:flutter/material.dart';

class EchoScreen extends StatelessWidget {
  const EchoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Echo Screen"),);
  }
}
