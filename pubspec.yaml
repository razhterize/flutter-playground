name: flutter_playground
description: "Testing application to learn about Drift with Dart."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

    # Core
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  hydrated_bloc: ^9.1.4

  # Settings/System
  path_provider: ^2.1.2
  path: ^1.8.3

  # Math Shit
  expressions: ^0.2.5+1

  # Misc
  logger: ^2.0.2+1
  equatable: ^2.0.5
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  intl: ^0.18.0
  money_formatter: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
